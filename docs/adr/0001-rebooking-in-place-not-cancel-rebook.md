# 改签采用原地更新（R1），禁止「先退票再重订」

用户换航班时，在同一 Booking 上更新 `flight_id` 与座位，**确认号不变**。Saga 顺序为：锁新座 → 快照旧状态 → 原地 UPDATE Booking → 释放旧座 → 结算差价。任意步骤失败则补偿回滚，原票始终有效。

曾考虑「先 Cancellation 再 New Booking」，但该路径在新订失败时原票已取消、用户无票可用，故明确不作为改签实现。
